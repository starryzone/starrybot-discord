const { buildBasicMessageCommand } = require('../../utils/commands');

module.exports = {
  removeRejection: {
    name: 'removeRejection',
    config: {
      content: '✨ 👍 🌟',
      done: true,
    }
  }
}
